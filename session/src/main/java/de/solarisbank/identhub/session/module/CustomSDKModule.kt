package de.solarisbank.identhub.session.module

import org.koin.dsl.module

/**
 * Can be used by a custom fork of the SDK to customize the behavior even more
 * Should be replaced by another file with actual custom module in it
 */
internal val customSdkModule = module {}